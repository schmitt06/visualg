algoritmo "teste"

var
  x, y, z, a, b, c, d : real

inicio

  x <- 2.0
  y <- 9.0
  z <- -7.0

  a <- Exp (x,y)
  b <- RaizQ (y)
  c <- pi
  d <- Abs (z)

  escreval (a)
  escreval (b)
  escreval (c)
  escreval (d)

fimalgoritmo
